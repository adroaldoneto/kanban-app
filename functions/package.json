{
  "name": "kanban-firebase-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5",
    "nodemailer": "^8.0.5"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
